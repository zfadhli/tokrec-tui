---
phase: 01-fix-render-child
plan: 01
plan_type: execute
autonomous: true
---

<objective>Fix "cannot render child" crash by storing actual Renderables instead of VNode proxies in addLogEntry()</objective>

<context>
@src/cli.ts:385-399 — addLogEntry() stores VNode proxies from Text() calls, but add() internally instantiates them into real Renderables. When remove() is called with the proxy, it fails instanceof BaseRenderable check and throws. The 51st log entry triggers this after 2-4 hours of runtime.
</context>

<acceptance_criteria>
## AC-1
Given the TUI has 50+ log entries, When a new entry is added, Then the oldest entry is removed without throwing "cannot render child"
</acceptance_criteria>

<tasks>
<task type="auto">
<name>Fix addLogEntry to store Renderables</name>
<files>src/cli.ts</files>
<action>After this.logPane.add(entry), get the actual Renderable via this.logPane.getChildren().at(-1) and push that to logEntries instead of the VNode proxy.</action>
<verify>Build passes, no TypeScript errors</verify>
<done>AC-1 satisfied</done>
</task>
</tasks>
