---
phase: 04-delete-user
plan: 04
plan_type: execute
autonomous: true
---

<objective>Add delete user function with 'd' keyboard shortcut</objective>

<context>
@src/cli.ts:210-231 — key handler: s→stop, r→restart, n→new, no delete yet
@src/cli.ts:112-118 — shortcuts guide text needs [d] delete added
@src/cli.ts:500-505 — stopSelectedUser() stops user and logs
@src/manager.ts:66-70 — stopUser() stops recording controller
</context>

<acceptance_criteria>
## AC-1
Given a user is selected, When user presses 'd', Then user is stopped, removed from config, and sidebar updates
</acceptance_criteria>

<tasks>
<task type="auto">
<name>Add deleteSelectedUser method and 'd' shortcut</name>
<files>src/cli.ts</files>
<action>1. Add deleteSelectedUser() method: stop user via manager.stopUser(), remove from config.users, save config, remove from sidebarTexts, update sidebar and detail. 2. Add 'd' key handler calling deleteSelectedUser(). 3. Update shortcuts guide text to include [d] delete.</action>
<verify>bun build --no-bundle src/index.ts passes</verify>
<done>AC-1 satisfied</done>
</task>
</tasks>
