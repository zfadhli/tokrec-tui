---
phase: 03-overlay-navigation
plan: 03
plan_type: standard
autonomous: true
---

<objective>Move stop/restart context into detail pane and remove overlay-based selection</objective>

<context>
@src/cli.ts:361-401 — updateDetail() renders 4 lines of info for selected user
@src/cli.ts:491-530 — handleStopMode() uses overlay with static list + text input (hard to use with large lists)
@src/cli.ts:532-575 — handleRestartMode() same overlay pattern
@src/cli.ts:212-235 — key handler: s→stop, r→restart already work on selected user
</context>

<acceptance_criteria>
## AC-1
Given a user is selected in sidebar, When viewing detail pane, Then [s] Stop and [r] Restart hints are visible at bottom

## AC-2
Given stop/restart hints are shown, When user presses 's' or 'r', Then action executes on selected user directly (no overlay)

## AC-3
Given overlay-based stop/restart is removed, When user presses 's' or 'r', Then no overlay appears
</acceptance_criteria>

<boundaries>
DO NOT CHANGE:
- Sidebar scrollable behavior
- Log pane ScrollBox
- Detail pane layout structure (just add lines)
- Config loading or Manager logic
</boundaries>

<tasks>
<task type="auto">
<name>Add action hints to detail pane</name>
<files>src/cli.ts</files>
<action>Expand detailTexts from 4 to 6 lines. In updateDetail(), set line 4 as spacer, line 5 as contextual "[s] Stop  [r] Restart" (dim if user not active for stop). Show only when a user is selected.</action>
<verify>bun build --no-bundle src/index.ts passes</verify>
<done>AC-1 satisfied</done>
</task>

<task type="auto">
<name>Remove overlay-based stop/restart</name>
<files>src/cli.ts</files>
<action>Delete handleStopMode() and handleRestartMode() methods. In key handler, change s→call manager.stopUser directly on selected user, r→call manager.restartUser directly. Remove inStopMode/inRestartMode flags and overlay code.</action>
<verify>bun build --no-bundle src/index.ts passes</verify>
<done>AC-2, AC-3 satisfied</done>
</task>
</tasks>
