---
phase: 02-scrollable-sidebar
plan: 02
plan_type: standard
autonomous: true
---

<objective>Make the user sidebar scrollable so it handles many users without overflow</objective>

<context>
@src/cli.ts:125-135 — Sidebar is a plain Box with no overflow handling. With many users, items clip beyond terminal edge. The log pane (lines 151-164) already uses ScrollBox successfully.
@src/cli.ts:245-253 — moveSelection() clamps selectedIndex but never auto-scrolls to keep selection visible.
@src/cli.ts:589-612 — addNewUser() appends Text directly to sidebarRenderable.
</context>

<acceptance_criteria>
## AC-1
Given 20+ users in config, When the TUI starts, Then all users are visible by scrolling the sidebar

## AC-2
Given the sidebar is scrolled, When the user presses up/down to change selection, Then the selected item scrolls into view
</acceptance_criteria>

<boundaries>
DO NOT CHANGE:
- Log pane ScrollBox behavior
- Detail pane layout
- Keyboard shortcut bindings (q, s, r, n)
- Config loading or Manager logic
</boundaries>

<tasks>
<task type="auto">
<name>Replace sidebar Box with ScrollBox</name>
<files>src/cli.ts</files>
<action>Change sidebar from Box() to ScrollBox({ scrollY: true, flexDirection: "column" }). No stickyScroll — sidebar scrolls from top. Update child extraction (line 178) to use getChildren() pattern like log pane.</action>
<verify>bun build --no-bundle src/index.ts passes</verify>
<done>AC-1 partial — sidebar renders with ScrollBox</done>
</task>

<task type="auto">
<name>Add scroll-into-view on selection change</name>
<files>src/cli.ts</files>
<action>In moveSelection(), after updating selectedIndex, calculate if selected item is above/below visible viewport and adjust scrollTop to bring it into view. Use sidebarRenderable.getChildren() to get item positions.</action>
<verify>bun build --no-bundle src/index.ts passes</verify>
<done>AC-2 satisfied</done>
</task>

<task type="auto">
<name>Fix addNewUser for ScrollBox</name>
<files>src/cli.ts</files>
<action>Update addNewUser() to add Text to ScrollBox content correctly, using the same add() pattern. Ensure new user appears and is scrollable.</action>
<verify>bun build --no-bundle src/index.ts passes</verify>
<done>AC-1 fully satisfied — dynamic users also scrollable</done>
</task>
</tasks>
